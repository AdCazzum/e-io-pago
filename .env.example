# XMTP Receipt Split Agent - Environment Configuration
# Copy this file to .env and fill in your actual values

# XMTP Wallet Configuration
# Generate these with: npm run gen:keys
XMTP_WALLET_KEY=your_private_key_here
XMTP_DB_ENCRYPTION_KEY=your_encryption_key_here

# XMTP Environment
# Use 'dev' for testing, 'production' for live deployment
XMTP_ENV=dev

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Database Path
# Path to store XMTP local database (must be persistent)
# Default: ./data/xmtp-db
# DB_PATH=./data/xmtp-db

# Optional: Node Environment
# NODE_ENV=development
